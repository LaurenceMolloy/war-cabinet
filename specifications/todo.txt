
NOTE for AI: do NOT implement anything in this file. 
This is simply an overflow for ideas that hit my brain 
while waiting for you to complete your tasks.I needed
somewhere to record those details so they don't get
forgotten.

I will explicitly feed you requests when I want you to
work on any of these ideas. Until then, do not touch
this file.

* headings on all pages to foloow Quartermaster/Mess Hall
* subtext on MEss Hall form needs to be UNDER headings rather than alongside it.
* instruction in each section needs a little spacing above
* instruction in each section needs finessing.

* default supplier for items in config
* I wonder if we should have a database of suppliers with ability to add your own (to add to memory like ingredients)
* entering item type - keyboard in the way
* remembering to go to config is a pain... it should be possible to spin up a new cabinet or item type on the fly from the main page.
* when selecting cabinets- we shopuld know which are freezer cabinets - it doesn't say
* freezer items - lifespan is off bottom of screen and looks empty
* when you re-use an item it must remember the last place you stored it and use that as a default
* save to stock is hidden partly behind on-screen buttons

we need to consider our intelligent batch merge - identifying when 
someone has manually entered a new batch, but it's actually the same
as an existing batch - e.g. they bought two packs of the same thing
and put them in different places. We should be able to merge them.
Supplier & Range are now relevant aspects of this match to consider.

We need to specify tests for Supplier & Range entry and suggestions
We need to check the test spec for default suggested locations logic

AUDIT MODE

We need to re-assess the backup and restore capability in light of
the new fields just added (Supplier/Range/BatchIntel).


I wonder if Front Line should be learned from usage in addition to
(or maybe instead of) configuration.

perhaps we can relegate batch intel/notes to a note icon that brings
up notes in a separate dialog - this allows more freedom with length
of notes and we can keep the main UI cleaner.


Iteration 84: Quantity Intelligence: Focuses on the "Accidental Zero" (e.g., entering 10 instead of 1). It uses a $>300%$ historical maximum trigger to catch stray characters.
Iteration 85: Thermal Intelligence: Focuses on "Climate Silos." It audits the match between Category (Ambient/Frozen) and Cabinet (Standard/Freezer) to prevent spoiled data.
Iteration 86: Dimensional Intelligence: Focuses on "Size Scaling" (e.g., your 5L vs. 1L example). It uses the $<50%$ or $>200%$ heuristic with a 3-batch "baseline establishment" period to avoid over-alerting on new items.
Iteration 92: Logistical Intelligence (I): Hardens Near Miss detection using full-word Levenshtein distance (D <= 2) and the "Elite 3" pooling rule to ensure vocabulary alignment across brands and ranges.
Iteration 93: Logistical Intelligence (II): Extends Elastic Typeahead (distance-aware scoring) and fuzzy harmonization modals to fresh ingredients and legendary chefs in the Mess Hall.

Other intelligence: mixing up g/kg and ml/l (factor of 1000 and float values perhaps used)


I still need to test the following:
1. recipe fuzzy suggestion and alignment
2. batch add suggestions (default/memory when empty)
3. merge rules for batches.
4. sensible alignment triggering on submit button - esp when multiple fields require alignment (I don't like repeated need to hit submit button)


batch add: i note that there is no range suggestion offered in this form?

generated recipe prompt page - header format seemsto have gotten corrupted

deal with partial consumption of items (5ltr oil being used 1ltr at a time)



MOBILE TESTING FEEDBACK
1. batch "expires"/"expired" needs to be replaced with the clock icon
2. FRZ => Frozen, LIM can be removed entirely
3. Config item editing, default batch and range don't have default suggestions
4. Error of omission when user forgets to set expiry (and gets current month)
5. deletion of user added suggestions is too easy - slight redesign might help
6. confirmation for user suggestion delete - items affected by it + do you just want to change or actually delete?
7. weekly or monthly reminder that manual backup hasn't been done - would they like to do one?
8. we might need a way to automate the sending of an email (free service?)
9. I can enter a batch WITHOUT a storage cabinet and the app says "Global - Storage". 
   I think we need to pick this up as an error of omission (like expiry date) 
10. I need to understand why location conflicts aren't always triggered when they should be.
11. batch add centralisation can include an expiry date OCR capability experiment (we have a working PoC).
12. recipes can take account of medical conditions or health focus (low fat, high protein etc...)
13. legal exclusions/warmings re. expired food and where responsibility lies!!!

I think we need to create standalone modals for the following, and I think we 
need to port existig ones over to the new ones, one at a time. we also need to
consider the state that may be caaried into the modal and the source of the call
so the user can be returned there afterwards.
- batch add
- batch edit
- item type add
- item type edit
- category add
- category edit
- cabinet add
- cabinet edit